<section class="otp-hero">
    <div class="otp-box">
       <form action="{{#if isPasswordUpdate}}/user/verify-password-otp{{else}}/user/verify-otp{{/if}}" method="post" id="otpForm">
            <div class="lock-icon">🔒</div>

            <p class="otp-text">
                Enter the 6-digit code sent to your registered device to authenticate.
            </p>

            <input type="hidden" name="otp" id="serverExpiryTime" value="{{otpExpiryTime}}">
            <input type="hidden" name="email" value="{{email}}"> 

            <div class="otp-inputs">
                <input type="text" class="otp" maxlength="1" inputmode="numeric" autocomplete="one-time-code">
                <input type="text" class="otp" maxlength="1" inputmode="numeric">
                <input type="text" class="otp" maxlength="1" inputmode="numeric">
                <input type="text" class="otp" maxlength="1" inputmode="numeric">
                <input type="text" class="otp" maxlength="1" inputmode="numeric">
                <input type="text" class="otp" maxlength="1" inputmode="numeric">
            </div>

            <button type="submit" class="otp-btn">VERIFY OTP →</button>

            <p class="resend-text">
                Didn’t receive the code? 
                <span id="timer-container">Resend in <b id="timer">01:00</b></span>
                
                <a href="/user/resend-otp?email={{email}}&isPasswordUpdate={{#if isPasswordUpdate}}true{{else}}false{{/if}}" 
   id="resend-link" 
   style="display: none; color: #ffffff; font-weight: bold; text-decoration: none;">
    Resend OTP
</a>
            </p>

            <a href="{{#if isPasswordUpdate}}/user/profile{{else}}/user/forgot-password{{/if}}" class="back-link">
                {{#if isPasswordUpdate}}
                    ← BACK TO PROFILE
                {{else}}
                    ← BACK TO FORGOT PASSWORD
                {{/if}}
            </a>
        </form>
    </div>
</section>

<div id="toast-container"></div>

<script>
document.addEventListener("DOMContentLoaded", () => {
    const inputs = document.querySelectorAll(".otp");
    const hiddenOtp = document.getElementById("fullOtp");
    const form = document.getElementById("otpForm");

    // Helper to update the hidden field
    const updateHiddenInput = () => {
        let otp = "";
        inputs.forEach(i => otp += i.value);
        hiddenOtp.value = otp;
    };

    inputs.forEach((input, index) => {
        // Handle Typing
        input.addEventListener("input", (e) => {
            if (e.inputType === "deleteContentBackward") return; // Let backspace handler take over

            if (input.value && index < inputs.length - 1) {
                inputs[index + 1].focus();
            }
            updateHiddenInput();
        });

        // Handle Backspace
        input.addEventListener("keydown", (e) => {
            if (e.key === "Backspace" && !input.value && index > 0) {
                inputs[index - 1].focus();
            }
        });

        // 3. ADDED: Handle Paste (Standard User Behavior)
        input.addEventListener("paste", (e) => {
            const data = e.clipboardData.getData("text").split("");
            if (data.length === inputs.length) {
                inputs.forEach((inp, i) => inp.value = data[i]);
                updateHiddenInput();
                inputs[inputs.length - 1].focus();
            }
        });
    });

    // 4. Client-side check before submit
    form.addEventListener("submit", (e) => {
        if (hiddenOtp.value.length !== 6) {
            e.preventDefault();
            alert("Please enter the full 6-digit code.");
        }
    });
});
</script>

