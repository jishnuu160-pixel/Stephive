<section class="hero">
  <div class="hero-text">    
      {{#if user}}
        <div id="welcomeToast" class="auth-card logged-in" style="position: absolute; right: 20px; z-index: 1000;">
        <p class="welcome-text">WELCOME, {{user.name}}</p>
        <p class="status-text">Ready to upgrade your collection?</p>
  </div>
      {{else}}
        <div class="auth-card">
        <p class="welcome-text">WELCOME USER</p>
        <a href="/user/login" class="login-btn">Log in</a>
        <a href="/user/signup" class="signup-btn">Create Account</a>
        </div>
      {{/if}}
    <h1 class="animated-title">
      Step into <br>
      <span class="hero-animated-title">Effortless <br>Style</span>
    </h1>

<p>
  <span class="fade-slide-text">Discover sleek sneakers crafted for comfort, confidence, and everyday wear.</span>
</p>
    <a href="/shop" class="btn">Shop Now</a>
  </div>
</section>

<section class="best-sellers">
    <h3>Latest Shoes</h3>
        <div class="best-sellers-shelf-grid">
            {{#each latestSellers}}
               {{> product-card}}
            {{else}}
                <p>No products available right now.</p>
            {{/each}}
    </div>      
</section>

<section class="best-sellers">
    <h3>BestSeller</h3>
        <div class="best-sellers-shelf-grid">
            {{#each bestSellers}}
               {{> product-card}}
            {{else}}
                <p>No products available right now.</p>
            {{/each}}
        </div>      
</section>